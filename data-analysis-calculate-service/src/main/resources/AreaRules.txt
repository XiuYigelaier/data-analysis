请根据以下数据分析此 GitHub 开发者擅长的领域，并给出相应的置信度，遵循以下规则：

如果接下来的对象中的 languages 有值，则开发者擅长的语言大概率是 languages 的值,且 credence为high。

返回值必须遵循格式：把你之前所遵守的规则的返回```json {"nation":"","credence":""}```改变为```json {"area":"","credence":""}```。请在其中填写您的推断值, 如果 area 无法确定，也必须返回相应的格式，而 credence 可以设置为 "N/A"。

其中"credence":""中返回为""为字符串不是数组例如"credence":"Vue,Rust,Python" 他每个项目的语言都可以成为credence的返回值

您可以通过开发者的名字或其博客的提示信息来辅助推测。

另外，您可以通过网络收集数据来进一步支持您的推测。例如，如果某开发者的名字为 "yyx990803"，我们可以推测他可能是尤雨溪，并且擅长 Vue.js。

返回的 area 值应以字符串形式列出，例如 "Vue,Rust"，并且根据开发者的编程语言能力进行排序。例如，Guido van Rossum 的 area 返回值中应将 "Python" 排在 "HTML" 前面，因为他是 Python 之父。

不需要提供推测的理由，直接返回格式即可，速度很重要。返回的格式为：```json {"area":"","credence":""}```，并在里面填写您推断出的值。

接下来是数据：

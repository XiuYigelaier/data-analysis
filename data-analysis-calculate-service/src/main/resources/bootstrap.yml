server:
  port: 9000
spring:
  application:
    name: data-analysis-calculate-service
  #  profiles:
  #    active: test

  cloud:
    config:
      discovery:
        enabled: true
        service-id: data-analysis-config  # 指定Config服务端在服务发现中的serviceId
      fail-fast: true
      uri: http://localhost:8762
      # 指定config server在服务发现中的serviceId，默认为：configserver
      name: config
xf:
  config:
    hostUrl: https://spark-api.xf-yun.com/v4.0/chat
    appId: 22af0843
    apiSecret: MWM5NDZjZjViZDVhYzEzMjM3NjBlMjA5
    apiKey: 257c0abf0e25d98e937507d957ad32eb
    maxResponseTime: 30
    domain: 4.0Ultra



---
spring:
  config:
    activate:
      on-profile: local
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/

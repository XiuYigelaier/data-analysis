  请根据以下数据分析此 GitHub 开发者的所在地区，并给出相应的置信度，遵循以下规则：

  返回值必须遵循格式：```json {"nation":"","credence":""}```。请在其中填写您的推断值，尽量准确推测 nation（国家/地区），如果 nation 无法确定，也必须返回相应的格式，而 credence 可以设置为 "N/A"。

      如果后续对象中的 location 有值，则返回的 JSON 对象中的 nation 属性应设置为 location 的值，且credence为high。例如，如果 location 为 "San Francisco Bay Area"，则返回的 nation 应为 "American"。

  您可以根据开发者的名字、或其博客所使用的语言进行推测。例如，一个中文名字可能暗示开发者来自中国，并且可能使用中文书写文档。

  另外，您可以通过网络收集数据来支持您的推测。例如，如果开发者的名字为 "yyx990803"，且擅长 Vue.js，我们可以推测他可能是尤雨溪，可能是中国人。

  不需要提供推测的理由，直接返回格式即可。请迅速给出结果，返回的格式为：```json {"nation":"","credence":""}```，并在里面填写您推断出的值。
